{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
      //"Microsoft.EntityFrameworkCore.Database.Command": "Information"//LINQでのSQL確認用に追加(Defaultがinformationなので現状は無くても出る)
    }
  },

  "_comment1": "開発時のローカル用",
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=nissy_dev;User=root;Password=root;"
  },

  "Seeder": {
    "Reset": false // ← true にしたら起動時にDBを初期データに初期化する。データ削除＆Sedderで再投入する
  }

}
